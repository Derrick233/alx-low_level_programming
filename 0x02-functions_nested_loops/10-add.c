#include "main.h"

/**
 * add - A function that adds two integers and returns the result
 * @num1: the first number
 * @num2: the second number
 *
 * Return: the executed number
 */
int add(int num1, int num2)
{
	return (num1 + num2);
}
