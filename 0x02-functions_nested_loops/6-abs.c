#include "main.h"

/**
 * _abs - This is a program that prints absolute value of a given integer
 * @value: this determines the integer to be inputted
 *
 * Return: The absolue value of a given integer
 */
int _abs(int value)
{
	if (value >= 0)
{
	return (value);
}
	else
{
	return (-value);
}
}
