#include <stdio.h>
#include "main.h"

void positive_or_negative(int i)
{
	if (a > 0)
		printf("%d is positive\n", a);
	else if (a == 0 )
		printf ("%d is zero\n", a);
	else
		printf("%d is negative\n", a);
}
